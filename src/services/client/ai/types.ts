/**
 * Re-export AI types for client-side use
 */

// Re-export types from the shared AI types
export type { 
  AIActionParams,
  AIResponse,
  AIActionResponse,
  AIHistoryItem,
  ChapterContent
} from '../../../types/shared/ai';
